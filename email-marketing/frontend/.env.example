# Sanity project ID
NEXT_PUBLIC_SANITY_PROJECT_ID=

# Sanity dataset name (usually "production")
NEXT_PUBLIC_SANITY_DATASET=production

# Sanity API token with Viewer permissions (read-only)
# Create at: sanity.io/manage -> Your Project -> API -> Tokens
SANITY_API_READ_TOKEN=

# Shared secret that gates the preview route (/api/preview/klaviyo/[id]).
# Callers pass it as ?sanity-preview-secret=<value> (or ?token=<value>).
# Required in production: when unset, the route refuses every request with a
# 500 and logs the reason. In development it may be left empty; the route then
# logs a one-time warning and serves previews without authentication.
# Generate one with: openssl rand -hex 32
SANITY_PREVIEW_SECRET=

# Klaviyo private API key — required for the Klaviyo preview route (/api/preview/klaviyo/[id])
# Without this, the route returns the Klaviyo-format HTML without calling Klaviyo's render API
# Create at: klaviyo.com -> Settings -> API Keys
KLAVIYO_API_KEY=
