# This package reads no environment of its own. SANITY_STUDIO_PROJECT_ID and
# SANITY_STUDIO_DATASET come from the starter root .env, which every workspace
# reads; the Studio that composes this plugin supplies the client.
