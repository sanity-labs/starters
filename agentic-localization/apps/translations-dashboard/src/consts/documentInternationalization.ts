export const DOCUMENT_INTERNATIONALIZATION_TYPES = ['article'] as const

/** Display labels for the translatable types. */
export const documentTypeLabels: Record<string, string> = {
  article: 'Articles',
}
